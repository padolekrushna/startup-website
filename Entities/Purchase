{
  "name": "Purchase",
  "type": "object",
  "properties": {
    "dashboard_id": {
      "type": "string",
      "description": "ID of purchased dashboard"
    },
    "buyer_email": {
      "type": "string",
      "description": "Email of the buyer"
    },
    "seller_email": {
      "type": "string",
      "description": "Email of the seller"
    },
    "amount_paid": {
      "type": "number",
      "description": "Amount paid in USD"
    },
    "payment_status": {
      "type": "string",
      "enum": [
        "completed",
        "pending",
        "failed",
        "refunded"
      ],
      "default": "pending",
      "description": "Payment status"
    },
    "transaction_id": {
      "type": "string",
      "description": "Payment transaction ID"
    },
    "download_count": {
      "type": "number",
      "default": 0,
      "description": "Number of times downloaded"
    }
  },
  "required": [
    "dashboard_id",
    "buyer_email",
    "seller_email",
    "amount_paid"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "buyer_email": "{{user.email}}"
        },
        {
          "seller_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "write": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}
